# RPS API (Pedra-Papel-Tesoura)

API simples em Node.js para estudar backend.  
Endpoints:
- `GET /health` — verificação de status (primeiro endpoint).

## Rodar local
```bash
npm run dev
# depois acesse http://localhost:3000/health
